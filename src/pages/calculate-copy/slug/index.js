/** @format */

import { Box } from '@mui/material';
export default function CalculateId() {
	// const router = useRouter();
	// const id = router.query.id;
	// console.log('router', router);
	return (
		<Box className="w-full">
			{/* <CalculatorForm id={id} /> */}
		</Box>
	);
}
